# Core analysis stack
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.7
pyarrow>=14.0          # parquet I/O for cache/

# World-model scripts (GRU / SIGReg-JEPA).
# Install the torch build matching your accelerator:
#   CPU   : pip install torch
#   CUDA  : see https://pytorch.org for the right index URL
#   ROCm  : see https://pytorch.org for the ROCm wheel
torch>=2.1
